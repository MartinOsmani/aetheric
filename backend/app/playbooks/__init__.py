"""Playbook tools — drop-in tool sets per project track.

- attribution: deep playbook, AI-native attribution agent (default winner).
- media_buying: shallow playbook, autonomous media-buying agent (fallback).

Each module exposes `all_tools()` returning a list of `Tool` instances for
the registry to consume.
"""
