"""Demo replay harness — placeholder.

The deterministic on-stage replay (canned event stream so the live demo never
depends on real-time LLM latency or randomness) ships tomorrow morning per
the plan. Tonight we drive the agent live against the stub or real LLM.
"""
